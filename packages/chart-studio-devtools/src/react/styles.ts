import devtoolsCss from './styles/devtools.css?inline'

/**
 * Full devtools stylesheet, inlined at build time for consumers (no separate CSS import).
 */
export const DEVTOOLS_STYLES: string = devtoolsCss
